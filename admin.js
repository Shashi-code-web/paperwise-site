document.querySelectorAll('.admin-panel .tiny-btn').forEach(b=>b.onclick=()=>alert('Connect this action to your authenticated backend before production.'));document.querySelector('.admin-panel .button')?.addEventListener('click',()=>alert('PDF upload requires private server-side storage. Do not upload production PDFs to a public GitHub Pages repository.'));document.querySelector('.cart-btn')?.addEventListener('click',()=>location.href='index.html');